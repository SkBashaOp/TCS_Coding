def binarySearch(arr,k):
  low=0
  high=len(arr)-1
  ans=-1
  while(low<=high):
    mid=low+high/2
    if(arr[mid]<k):
      low=mid+1
     elif(arr[mid]>k):
       high=mid-1
     else:
       ans=mid
       break
   return ans    
      
    
